# Hermes Project Guidance

## Project identity
- Project: Jeeves / MeshCore PathBot
- Repository: `https://github.com/eastmesh/jeeves`
- Checkout: `/home/hermes/workspace/repos/jeeves`
- Default branch: `main`
- Current baseline revision: `345aafce7a1f04d51f2df2d995fc9ba3539ea713`

## Current task boundary
Implementation is approved for the local fix only: make channel scope explicit per send, serialize scope/send/restore operations, add per-channel scope configuration, and improve reconnect/error robustness with tests. Do not commit, push, open a PR, alter branch protection, deploy, or send live radio traffic without coordinator approval.

## Working commands
```text
uv sync --extra dev
uv run pytest -q
uv run ruff check .
uv build
codebase-memory-mcp cli --json index_repository --repo-path . --mode moderate
```

## Architecture and safety constraints
- Keep radio operations async and preserve existing connection/reconnect behavior.
- `set_flood_scope` changes global companion-radio state; channel sends must not assume scope state without serialized, verified ownership.
- Treat MeshCore API behavior and firmware compatibility as external boundaries; test success and error envelopes explicitly.
- Keep secrets and live configuration out of source control.
- Preserve admin/guest UI parity and existing path/hash handling unless the approved plan says otherwise.

## Handoff requirements
Before coding, provide: observed reproduction/evidence, root-cause hypotheses with uncertainty, affected files and interfaces, acceptance criteria, rollback path, and unchanged scope. After coding, inspect the complete diff, run all relevant checks, refresh the code index, and obtain independent review before any remote action.
